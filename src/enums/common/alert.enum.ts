/**
 * `enum` for all available `IAlert` types:
 * -    `success`: positive message.
 * -    `info`: informative message.
 * -    `error`: negative message.
 */
export enum EAlert {
    SUCCESS = 'success',
    INFO = 'info',
    ERROR = 'error'
}
