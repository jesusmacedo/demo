/**
 * `interface` for defining a `CreateAccount` object.
 */
interface ICreateAccount {
    readonly firstname: string;
    readonly lastname: string;
    readonly email: string;
    readonly password: string;
}
